name: ScrapeBadger
targets:
  ScrapeBadger:
    type: framework
    platform: iOS
    deploymentTarget: "11.0"
    sources: [ScrapeBadger]
    info:
      path: ./Info.plist
      version: 0.1.0
    settings:
      APPLICATION_EXTENSION_API_ONLY: true
    scheme: {}
    dependencies:
    - carthage: AnyCodable
