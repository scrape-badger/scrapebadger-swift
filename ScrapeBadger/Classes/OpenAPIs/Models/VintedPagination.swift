//
// VintedPagination.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Page metadata. Resend &#x60;time&#x60; to page a search deterministically. */
public struct VintedPagination: Codable, JSONEncodable, Hashable {

    public var currentPage: Int
    public var totalPages: Int
    public var totalEntries: Int
    public var perPage: Int
    public var time: Int?

    public init(currentPage: Int, totalPages: Int, totalEntries: Int, perPage: Int, time: Int? = nil) {
        self.currentPage = currentPage
        self.totalPages = totalPages
        self.totalEntries = totalEntries
        self.perPage = perPage
        self.time = time
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case currentPage = "current_page"
        case totalPages = "total_pages"
        case totalEntries = "total_entries"
        case perPage = "per_page"
        case time
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(currentPage, forKey: .currentPage)
        try container.encode(totalPages, forKey: .totalPages)
        try container.encode(totalEntries, forKey: .totalEntries)
        try container.encode(perPage, forKey: .perPage)
        try container.encodeIfPresent(time, forKey: .time)
    }
}

